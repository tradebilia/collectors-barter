The uploaded MemberDirectory2.zip contained exactly one file, `Member Directory 2.svg`, with a valid SVG signature and embedded animateTransform. An unchanged copy was uploaded to durable WebDev storage as `/manus-storage/MemberDirectory2_c8de2e96.svg`, and MemberSearch now references that SVG directly; the approximate AnimatedHeroTitleArtwork overlay is no longer used on Member Directory. Browser content confirmed the exact SVG URL, and desktop/mobile screenshots showed the matching wheel-and-MEMBER DIRECTORY artwork. The screenshot tool freezes CSS/SVG animation at capture time, but the source retains its embedded animation declarations.

