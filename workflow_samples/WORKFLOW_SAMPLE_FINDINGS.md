# Workflow sample findings

The first two finished-post samples demonstrate the intended contrast: automatic subject interpretation adds Oakland-era green-and-gold context and more specific stadium detail, while smart routing stays neutral to the Baseball single-card category and avoids player/team-specific cues. Both preserve the real card on the left and use the right side for environmental references.

The automatic source background incorrectly rendered small percentage labels at the bottom edge, so it requires one focused cleanup before delivery. The smart-routing sample is readable and suitable for comparison.
